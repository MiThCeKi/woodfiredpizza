
function hideElementOnClick() {
    // I know this is against the DRY principle. Can we discuss how to fix this?
    // I want the click to call one query selector which is associated with one class. 
    // Is it possible to grab the class name from the menu and insert it in the query call?
    var clickedElement = document.querySelector(.)
    var clickedElement = document.querySelector(.)
    var clickedElement = document.querySelector(.)

}